<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lhh.seamanrecruit.dao.ResumeDao">


    <select id="selectPageList" resultType="com.lhh.seamanrecruit.entity.Resume">
        SELECT * FROM RESUME WHERE user_status in (1,2)
    </select>

    <select id="queryByResumePage" resultType="com.lhh.seamanrecruit.dto.resume.ResumePositionResDto">
        SELECT * FROM RESUME  WHERE user_id IN (
            SELECT user_id FROM user_position WHERE position_id IN (
                SELECT id FROM POSITION WHERE company_id = (SELECT id FROM company WHERE user_id = #{param.userId})) AND delivery_flag = 0)
    </select>

</mapper>